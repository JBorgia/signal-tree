{
  "//target": "target/lib are raised ABOVE tsconfig.base.json here, deliberately. Specs run under vitest on the .nvmrc Node; the base config pins the library's ES2015 SHIP floor, and those are different questions. The serialization security spec reaches for `Object.hasOwn` and BigInt literals to build hostile inputs, and typechecking them at es2015/es2021 produced 7 errors about the toolchain rather than about the code. Raised HERE ONLY — nothing about the published target changes.",
  "//": "Typechecks SPEC files, which tsconfig.typecheck-all.json excludes and core's tsconfig.typecheck.json covers only for *.typing.spec.ts. Consumed by tools/check-spec-types.mjs as a RATCHET, not a clean gate — see that file for the baseline and why.",
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,
    "target": "es2022",
    "lib": ["es2022", "dom"],
    "skipLibCheck": true,
    "types": ["node", "vitest/globals"],
    "allowJs": false,
    "strict": true
  },
  "include": ["packages/*/src/**/*.spec.ts", "packages/*/src/**/*.test.ts"],
  "exclude": ["**/node_modules/**"]
}
