# SignalTree audit and remediation — full handoff

Prepared September 24, 2026, America/Denver. This is the continuation entry point,
not a release approval. The user explicitly requested a full history and handoff.

## Read this first

The assignment is to research, understand, compare real repair/rearchitecture
options, and address **all** reproduced issues. The user repeatedly rejected
stopping after a partial checkpoint. Continue independent work while specific
public decisions are pending. Do not call everything blocked by those decisions.

**Release, registry changes, tag/push, advisory publication and deployment are on
hold.** No new architecture has won. Frozen L1–L18 and their oracles must not be
changed to make the incumbent pass. Conservative refusal is containment, not
successful surgical reversal.

Main is a large dirty working tree at
`7ade0e3ecb25ff0d06da4355b5f7d67844e147b7`, not a verified exact-SHA candidate.
No commits/version bumps were made by this repair pass. Preserve unrelated work.
`tools/experiments/` contains user-owned untracked research: read it, but do not
edit, sweep into a commit, or overwrite its results.

Read the repository AGENTS.md and .cursorrules before changes. For architectural
work, independent adversarial review is required at row boundaries; give reviewers
different bounded premises, not the author's desired conclusion. Final writes
and architectural authority stay with the primary agent.

## Immediate continuation

1. Read the isolated Record candidate report below. **Do not promote its patch.**
   It identifies concrete causal-membership and allocation-atomicity gaps, not
   merely hypothetical API uncertainty. Continue tests-first in isolation.
2. Independently qualify the nested-ancestor absence failures; stale held reads
   already violate an existing law. Do not silently reconstruct retained siblings
   when writing through a removed parent.
3. Await owner answers on recovery handle, live history policy, and hosted POST.
   These were asked; no answer is recorded. Other compatibility/product questions
   also remain—those three were immediate choices, not an exhaustive declaration
   that all future architecture decisions are settled.
4. Maintain the frozen semantic suite and mutation accounting. Existing ordinary
   tests pass while 68 stronger semantic cases violate and 168 are unsupported.
5. After a concrete repair: focused red/green, independent review, broader tests,
   fresh build, installed tarball checks, and evidence hashes. Preserve first reds
   and invalid harness attempts separately. Do not rerun-until-green.

## History and why the release is held

This section summarizes prior conversation evidence. Old release results are
historical, not freshly verified registry/remote claims.

### Original v16 release work

The original program audited multi-writer interactions and pursued a breaking
v16 API reset. `transaction()` became `transact()` and unshipped `proposal()`
became `propose()`, with no alias. The support policy explicitly recorded the
one-time deprecation-path exception. Current docs were migrated; history was
preserved. A global textual rename was explicitly rejected as proof of completion.

The generated invocation gate grew to cover functions, methods, callable
properties, callable types and constructable types, with normalized signatures,
deduplication and 13 mutation controls. This protects current exported forms,
not all conceivable future TypeScript patterns. `36efda16` closed that extension.

React pending-state realization was repaired in `429c7fff`: owner observation
follows coherent current truth; durable consequences remain settlement-gated;
successful rollback compensation invalidates restored truth. Raw transact A–D
controls, all four Phase B 7/7 contracts and carrier mutations were retained.
Do not discard this separation merely because subsequent transaction defects
were found. The old settlement-oriented observation law was explicitly superseded.

The public agent-review reference (`edd5d8f5`) demonstrated current/superseded
contribution review without private identity/effects or new primitives. Framework
write grammar differs intentionally: Angular/Solid .set(), Vue .value,
React/kernel callable writes. An application's readByPath is not a universal
library resolver; later audit proves public path ambiguity.

Retention research refuted the old cross-process slope/ratio verdict. Runtime
modes were localized to large_object_space; cross-process inversions did not
prove physical impossibility. Same-arm strong retention of retired-node handles
characterized a gross-retention tripwire, not tiny per-subject leaks. Linux run
35886271808 at `f8f815053a1b7722d2389bddb088100135880733` measured 30 controls
3.23–3.24 MB and ten retain=10k samples 82.75–82.76 MB. This independently
validated the preexisting 40 MB ceiling in that environment. Do not generalize
the observed Mac/Linux difference into an OS cause or waive unrelated red gates.

Historical RC sequence:

| Candidate | Disposition |
| --- | --- |
| 9d2c7557ff25c69554e0878e5cfdfa8dadde5200 | 83/84; stale live version claim; superseded |
| 4ce598a2ebe68b37a67d2694b13196de467178f1 | 84/84 but 72/73 proven, one unproven despite self-test exit zero; superseded |
| db3da56eadb0bc94c4703bf750b5d8679e6e2659 | Added missing proof; superseded for fail-closed harness correction |
| 9b2aa2540f0b6062c1e722e92879cd6ec27ad4e4 | Historical local 84/84 and 73/73, clean rebuilt artifacts and exact-SHA remote verification; release subsequently stopped by owner |

The self-test harness was changed so unproven/blind/errored results fail. Mutation
testing must precede a clean artifact rebuild; clean git status cannot certify
ignored dist. The conversation reports the v16 tag/release were removed and npm
remained on the old release; do not republish from the old workflow. Current
remote/registry state has not been re-queried for this handoff.

### Product reset and falsification

The owner stopped release and requested product/architecture reconsideration,
including the null option: remain a good state library. Broad accountability
differentiation was not earned; cross-runtime causality requires transported
evidence, not inference. No Relay, MCP, actor subsystem or new Studio features
were authorized by a missing capability.

R6 showed failed structural rollback could discard pending authority while
leaving speculative x=1 live and unconfirmed; later reject falsely succeeded as
a no-op. It reproduced through raw transactions, not only Proposal.
R8 overlapping pending scalars showed rejecting the older writer could clobber
the newer one, and rejecting both could resurrect already rejected truth.
**A captured baseline is not ownership.** These falsified the current product
claim; do not patch around the examples and call the architecture proved.

P1–P18 probes additionally exposed composition failures: dotted addresses,
refetch, whole-tree consequence holds, deferral losing context, and transaction
retention. Composed semantics took precedence over a narrow competitor contest.
TanStack remains a serious control, not a straw man; its normalized row model
is a different product and must be executed fairly. No external source claim
should be treated as current without checking the pinned/current source.

Published-v15 audits in prior work reported R6/R8 across stable 15.0.0–15.2.1.
The hotfix direction became safe atomic refusal, preserving v15 API, without
pretending that containment solves multi-owner architecture. No hotfix was
published by this session.

### Frozen semantics, before architecture

The owner repeatedly refined the laws before executable contracts. Final
constitution is `docs/research/transaction-semantics-2/{LAWS,MATRIX,MUTATIONS,
PREDICTIONS}.md`. There are 18 laws; an earlier nineteenth grep match was the
freeze banner, not another law.

The laws cover ownership, conserved settlement authority, removing contribution
instead of restoring baseline, newer truth, no resurrection, authored precedence,
lifetime versus key, structural dependency, atomic disposition, honest inspection,
canonical authority, authority ordering, coherent publication, explicit retry and
terminality, responsibility-bounded retention, independent progress, lossless
identity, and context-preserving deferral.

Critical refinements: settlement time does not reorder contributions; atomic
disposition does not require every superseded field to win; a fresh authoritative
snapshot advances canonical truth but does not itself settle a local proposal.
Authority order, truth payload and settlement relation are orthogonal. Numeric
test revisions have explicit order. The adapter exposes readCanonical separately
from readVisible and observes coherent snapshots. Do not synthesize missing
candidate capabilities in adapters to manufacture passes.

### Security and full local audit

An external session reported scanning all 141 @signaltree/core versions:
20 clean, 107 code-bearing vulnerable versions from 3.1.0–14.1.3, and 14 empty
publishes. **That full census is owner-supplied evidence, not independently
repeated by this session.** Local legacy resolver/security regressions and built
artifact controls were independently exercised. Exact deployed exposure remains
unproved. Static TruckTrax/86xed scans found affected dependencies/resolver but
no enabled serialization/persistence input route in inspected application scope;
that is not a deployed security clearance.

The proposed public advisory/deprecation/publish sequence remains private/on hold.
Legacy resolver repair, dead current codec removal, v15 containment and the
current comprehensive audit were handled separately. The full chronological
record is `2026-09-23-remediation.md`; it preserves regressions found after earlier
green checkpoints, harness errors, corrected interpretations and validation.

## Current workspaces and verified artifacts

| Workspace | Base and version | Status |
| --- | --- | --- |
| /Users/jonathanborgia/code/signaltree | 7ade0e3ecb25ff0d06da4355b5f7d67844e147b7; 16.0.0 | Large uncommitted current audit; release held |
| /private/tmp/signaltree-15-safety | cde208a0909442ec1e5c61a1f9ff3ead683f5619; codex/v15-safety; 15.2.1 | Patched unpublished test artifact; not numbered 15.2.2 |
| /Users/jonathanborgia/code/signaltree-14x | 5028433eb21ef253db85bb3cfc2fc89acfb8cfd5; release/14.x | Actual maintenance checkout with reviewed fixes and additional untracked test noted below |
| /private/tmp/signaltree-14-security | same legacy base; 14.1.3 | Isolated legacy qualification copy |

### Main latest qualification

`/private/tmp/main-audit-catalogue-final/` records fresh uncached all-package
build exit zero, no captured input drift, all five versions 16.0.0. Kernel:
**315 files, 2,734 passed, six expected failures, 13 skipped, zero todo**.
Typecheck, spec-type gate, kernel lint, invocation, error codes, declaration docs,
package hygiene and dev-mode checks exited zero.

The unchanged budget gate now passes: bare **9.75/10.25 KB prod,
11.89/12.45 KB dev**; entities **22.39/22.60 KB prod, 25.02/25.25 KB dev**.
The repair removes 26 unused entries from a private frozen diagnostic catalogue,
retaining three actual messages/guards. Historical code documentation remains
with corrected emission status. Independent review and isolated full tests
preceded promotion. Do not use the slightly different isolated 22.40/25.03
measurements as the actual-main figures.

`/private/tmp/catalogue-final-consumer/result.json`: strict skipLibCheck:false
bundler/node16 consumers pass; four unchanged facade fixtures retain 16 negative
type controls; four facade runtime and six original own-property controls pass.
All 144 dist files stayed unchanged. Kernel tarball SHA-256:
`22e584aeeed0830fc5a217c6ba189966debba0164a74e8d063d58b7ccd9b14a0`.

Prior full adapter checkpoint: Angular164+3skip, React20, Vue46, Solid26 and
React reference26 passed. This predates the private catalogue cleanup; do not
mislabel it as rerun with the latest bytes. The latest packed facade controls
are fresh. Previous main full checkpoint was 2,721 passed; 13 permanent authority
instrument tests account for the latest increase.

Previous main artifact
`92d3b949b1ed87d86baa3be50a98aacbbfd3a7fa95d7a03a94ce82033a68c7f7`
and its red budget are historical and preserved.

### Patched v15

`/private/tmp/v15-own-property-qualification/RESULTS.md`: 292 files,
2,487 passed, seven expected failures, 13 skips, zero todo; typecheck/lint/build
zero. First new-fixture spec typing errors were preserved and repaired without
changing runtime oracles; spec gate then zero with baseline 223 known errors in
36 files unchanged. Budgets pass its unchanged ceilings.

Installed tarball controls: prior 36 byte-identical runtime controls, 20
own-property controls, three public omission controls, four GC controls, strict
bundler/node16 types pass. Inspection has one pass/two unsupported; v16 Proposal
capability was not invented in v15. 89 installed/dist files matched.

Artifact `/private/tmp/v15-packed-own-property-final-consumer/signal-tree-kernel-15.2.1.tgz`
SHA-256 `7e5e8d288daf96174eea315fbb577b97e8cb6a095c2ef5c7302bfc5a744c84cf`.
It is intentionally still an unpublished patched 15.2.1 fixture.

### Patched legacy

Reviewed production files: core serialization.ts and entity-signal.ts. Reviewed
new suites: serialization.security.spec.ts and entity-own-property.spec.ts.
Both isolated and actual maintenance checkout latest qualification: **1,205
passed, 15 skips, 102 files, exit zero**. Known wider typing debt remains;
do not declare a fully clean legacy workspace.

Installed artifact: ten security/reference, six cycle and 24 own-property
controls pass. Independent 50 controls: baseline28held/22violated to candidate50held.
Artifact `/private/tmp/signaltree-14-security/artifacts/own-property/signaltree-core-14.1.3.tgz`
SHA-256 `818fbef4d398c0f70f884e630ad9b784d54007ef2b0320d6cce60b9c110e19e4`.
Own-property repair preserves own getters and fresh uncached updater reads.

**Handoff-time fresh status additionally finds**
`packages/core/src/enhancers/serialization/independent-exploit.spec.ts` untracked
in the actual 14.x checkout. Its origin and participation in the earlier full
run were not established. Do not delete, stage or attribute it to the reviewed
four-file patch without inspecting ownership. The previous four-file byte-match
record does not account for it.

## Repairs in the dirty main tree

Use the chronological ledger and nearest red tests for intent. Major groups:

- Transaction failed compensation retains authority/hold; unsafe newer-pending
  overlap refuses atomically; same-tick and ABA evidence uses queued witnesses
  without invoking callbacks to discover truth; terminal destroy/throw-undefined
  handling and queued inspection chronology have controls.
- Restoration addresses, compensation provenance, unrelated confirmation,
  redo truncation, pending overlap and opaque terminal payloads have regressions.
- Link scheduling is relationship-specific; held/displaced waiters, exceptions,
  disposal, reorder, structured addresses, materialized snapshots and in-flight
  permission bypass were repaired. Tree-wide hold remains; L16 is not solved.
- Batching captures enqueue-time semantic context and structured location identity,
  intercepts lazily materialized fields and refuses unsupported scope escape
  before scheduling. Native carrier and thrown-observer tests found further reds
  and repairs after the first batch was green.
- Own-property entity field boundaries, derived disposal/equality, dormant direct
  fields, observation lifecycle, native tooling arguments and snapshot types were
  audited across facades. Nested absence remains separately red.
- Dead unexported current serialization was removed after reachability/replacement
  investigation; legacy codec remains separately maintained. Do not restore the
  removed current source just because there are many deleted files.
- Shared declaration identity graph repairs strict installed-consumer nominal
  identity and hydration/Link admission; no broader Link admission was approved.
- Spec-type tooling fails closed on compiler infrastructure errors. Callable gate,
  publisher exact-candidate artifact/provenance checks and version claims have
  mocked/isolated regressions. This is not live publisher qualification.
- Benchmark detail GET validates owned benchmark records/IDs/content boundaries;
  no real service was attacked. POST authorization remains undecided.
- Latest documentation admits public inspection-path ambiguity; demo readByPath
  is explicitly fixture-specific. No value snapshots or new public address added.

## Stronger semantic and mutation evidence

Latest captured current conformance:
`/private/tmp/signaltree-final-current-conformance-ownproperty-20260924/RESULTS.md`.
**424 cases: 188 held, 68 violated, 168 unsupported, zero execution errors.**
Four commands exit one. Scalar25/9/1, structural135/48/24,
composition28/10/133, authority0/1/10 (held/violated/unsupported).
All verdicts/assertion arrays match the preceding capture; catalogue cleanup
postdates this capture. S13/local-frontier/R remains baseline-held →
current-violated because conservative refusal fails required successful reversal.

`2026-09-23-mutation-coverage.md` is the accounting authority:
**17 counted source-mutant variants / 15 IDs**:
M01,M02,M03,M04,M05,M06,M08,M10,M14,M15,M20,M22,M24,M25,M26.
Two M03 and two M24 variants explain the count. M01–M30 is not closed.
Already-red assertions, compilation failures, unsupported rows, marker-only
instrumentation and scripted returns do not become production kills.

Recent additions: M04 actually changes confirmation precedence; M06 numeric
business key/lifetime confusion retargets held old references; M08 actually
empties enqueue metadata, distinct from M25 reading context at execution.
Control → targeted semantic red → restored green and input hashes are retained.

M18 heterogeneous publish:false→true executed the target branch but survived:
six held/two unsupported case rows, 38 held/two unsupported assertions in all
three phases. No kill. M19 removed a terminal guard and produced an extra
unchanged-state notification, but no required state mutation: useful bounded
publication evidence, **uncounted as named M19**.

13 new permanent authority trace tests plus previous28 total41 pass. They use
strict finite scripted traces, validate actual truth/revision/read call protocol,
and kill bad arrival ordering at the instrument level. They do not implement
native authority semantics or count as production-source mutations. The frozen
prerequisite does not demand impossible current-source mutations of nonexistent
capabilities before comparing future candidates; use honest evidence categories,
not a circular requirement or synthetic current conformance.

Six expected failures in ordinary main suite = **two public open-key defects plus
four unexported provenance experiment limitations**. Do not advertise a public
provenance capability or silently treat expected failures as repaired.

## Latest unfinished candidate — supplied Record values

Source copies:
`/private/tmp/open-key-supplied-baseline` and
`/private/tmp/open-key-supplied-candidate`.
Read `evidence/RESULTS.md`, `PREREGISTRATION.md`, raw logs, integrity file and
`candidate-NOT-FOR-PROMOTION.patch` in the candidate directory.

This report was discovered on disk during handoff; the prior worker ID is no
longer reachable. Primary checked the full-final summary and compared scratch
files with main. Two production files differ only in scratch: signal-tree.ts
and internals/materialize-markers.ts. Two new scratch specs exist. Main has
not received the candidate. Do not assume the scratch implementation was reviewed.

Existing law already requires complete Record values to preserve supplied legal
keys. This target is **not** waiting for a product decision. Candidate reuses
canonical classifier, position/leaf construction, accessor/store pairing and
membership publication, with prepare/install separation. No proxy/shadow store.
It is real partial feasibility evidence, not a safe repair:

| Result | Evidence |
| --- | --- |
| Final new controls on baseline | 35 failed, 7 passed, two original expected failures across selected files |
| Candidate new controls | 33 pass, five fail out of 38 |
| Candidate full kernel | 2,757 passed, 16 failed, five expected failures, 13 skips; 317 files |
| Source/new-spec strict typecheck | Zero |
| Native framework observations | Ten baseline errors become ten correct observations across five facades |

The five load-bearing new failures:

1. Newly inserted ordinary membership survives transaction rollback.
2. Undoable insertion does not create undo authority.
3. A preexisting Link misses insertion.
4. Pending insertion remains silent even after confirmation.
5. Failed preparation leaves an unreachable scalar allocation (slot count1→2).

Later field writes on the inserted child rollback/undo/Link correctly. It is the
**absence→presence membership effect** that lacks causal capture/realization;
snapshot invalidation alone is not an authored reversible effect. Existing
entity structural recipes are subject-keyed. Do not fake ordinary branches as
EntityMap subjects or add root snapshot/shadow authority to pass tests.

Preparation protects public values/publication in two throwing-payload controls,
but canonical allocations happen before later preparation can fail; a discard or
allocation-atomic construction mechanism remains unproved.

Eleven other full-suite failures need honest disposition, not bulk deletion:
one expected-failure target now passes, five old Record discard characterizations,
three ST2010 discard diagnostics, one schema-mismatch discard test and one
prototype-pollution test that also expects a later explicit own key to disappear.
The latter does not establish renewed prototype pollution. Types are erased;
supporting new keys also affects cast/untyped closed-shape input. Do not invent
an opt-in/schema/type narrowing to avoid measuring compatibility.

Exploratory direct-source size grows ~370B prod/~363B dev. This is not an
authoritative dist budget run. Retained construction closures/key indexes need
lifecycle/cost qualification. Missing-before-first-write remains unresolved.

## Separate open-key and nested-absence evidence

`/private/tmp/open-key-contract-audit/REPORT.md`: 65 public facade observations,
40 collision observations. Complete root/branch/updater/new nested keys are
discarded on the incumbent. Seeded native carriers work. Missing-before-write
cannot rely on erased Record value type to classify a future handle. Native
name/call/bind/then collisions and Promise assimilation are discriminators,
not permission to blacklist valid ordinary keys or weaken public types.

`/private/tmp/nested-absence-independent/RESULTS.md`: **39 tests,17 pass,22 red**.
Held descendants read old storage after an ancestor is omitted; writes remain
hidden; updaters receive retained value rather than semantic undefined. Seeded
Record typed read/omit/write compiles across all five facades (source strict,
skipLibCheck:true). Closed optional objects also reproduce at runtime, but types
map Row|undefined to a Leaf; nested .n yields five TS2339. Do not present that
optional example as fully typed nested API. Vue .update was excluded because
public leaves are Ref; its .value remains tested.

Three of those reds are separate immediate-leaf native absence controls on
Angular/Vue/Solid. Existing member-membership law and whole-value cases7/14
require undefined reads and dormant activation. No general refusal policy was
found. Reconstruction of a wholly omitted ancestor with required siblings is
not uniquely specified: neither silent hidden writes nor automatic sibling
resurrection is justified. Preserve first harness errors separately.

## Owner choices and options

Asked, unanswered:

1. Add optional recovery handle to existing error when callback throws and
   compensation refuses? Recommended yes; same still-pending transaction,
   original error evidence preserved. Catch-inside works only prospectively.
   Optional draft shape was recovery:{transaction,callbackFailed:true,callbackError};
   no implementation approved. Observer errors must not be mislabeled callback
   failures; thrown undefined requires an explicit failure flag.
2. Separate bounded opt-in diagnostic history from correctness retention?
   Recommended yes. Reader-visible history policy cannot silently change.
   Destroy cleanup is already proved; live retention is not fixed by it.
3. Remove unused benchmark POST or keep with real authentication? Recommended
   removal if not needed; no owner answer. Anonymous with abuse controls remains
   another product option. CORS, Origin and sessionId are not authentication.

Additional boundaries in the decision table include independent outbound progress,
production diagnostic folding, lossless public inspection addresses and absent
Record handles. Literal independent writes already violate L16; arbitrary JS
dataflow cannot be inferred merely from distinct write locations. Bundle increase
is **no longer a necessary choice**: unchanged budgets now pass.

Architecture options are detailed in `2026-09-23-options.md`: conservative v15
containment; live contribution frontier; isolated draft/merge; hybrid committed
frontier with structural dependencies; null/state-library-only. No winner.
Previously preserved executable models in tools/experiments/transaction-options
were hash-verified (25 sources,32 reports) but neither changed nor rerun:
prepared261pass/332fail/47unsupported, frontier614/0/26, draft55/0/4,
replay109/0/531, with different excluded profiles. **Not a leaderboard.** Separate
attacks found retention/ordering/ACK limits; no native composition was proved.

## Evidence index and tooling cautions

- Original audit: `/private/tmp/signaltree-audit-7ade0e3e/AUDIT.md`.
- Main latest build and checks: `/private/tmp/main-audit-catalogue-final/`.
- Main packed results: `/private/tmp/catalogue-final-consumer/`.
- v15: `/private/tmp/v15-own-property-qualification/RESULTS.md`.
- Legacy: `/private/tmp/signaltree-14-security/artifacts/own-property/RESULTS.md`.
- Recovery: `/private/tmp/transaction-recovery-review/RESULTS.md`.
- Hosted boundary: `/private/tmp/hosted-benchmark-boundary-private/DRAFT.md`.
- Bundle independent review: `/private/tmp/bundle-catalogue-independent-review/REVIEW.md`.
- Authority instrument: `/private/tmp/authority-trace-permanent-validation/RESULTS.md`.
- Mutation roots: `/private/tmp/signaltree-isolated-M*/`; use inventory for exact proof.

Use pnpm/Nx, read exit codes rather than successful-looking tails. Rollup emitted
output but retained handles in prior runs; canonical build with --forceExit
exited after awaited work. Initial interrupted builds remain incomplete evidence.
Avoid bare Vitest at root where framework setup differs. Semantic runner accepts
`node tools/run-semantics-supplemental.mjs current NEW_OUTPUT.json [structural|composition|authority]`;
default scalar. It intentionally refuses to overwrite evidence. Expected red
conformance exit one is not an infrastructure failure.

Do not run mutation tools against final packed artifacts. Use isolated copies,
then clean-rebuild before artifact qualification. Compiler/load/timeouts do not
count as semantic kills. A prior M18 delegate was interrupted by a content flag;
that interruption is not product evidence and must not be retried to bypass a
restriction. Its separately preserved resolver error/actual local run have
explicit dispositions in the mutation inventory.

## Handoff inventory and stale pointers

At handoff the primary recorded fresh HEAD, tracked diff and full untracked
status. The snapshot bundle under `artifacts/audit-handoff-2026-09-24/` is ignored,
private local recovery material, **not generated metrics authority**. It contains
working-file snapshots/manifests, patch/status and selected evidence; original
raw /private/tmp paths remain the detailed authority. Excludes user research
tools/experiments from source snapshots. Do not restore a whole patch blindly
over user changes; files may have mixed ownership.

Known stale summaries: remediation's top mutation count still said14/12 before
this handoff; mutation inventory correctly says17/15. Older checkpoint sections
and AGENTS budget prose still describe the preserved red build; the latest
checkpoint addendum and actual budget log supersede those figures, not ceilings.
The open-decisions table still includes a now-resolved budget row and conflates
supplied-value insertion with absent-handle design. Read this handoff and the
new concrete reports before treating those rows as blockers.

Former workers (IDs are historical; send_input failed 'agent not found' at
handoff, so availability is not promised):

| Worker | ID | Last responsibility |
| --- | --- | --- |
| Tesla | 01a0d0bf-ebf1-77d3-8822-bd2628072798 | Bundle repair and latest packed qualification, complete |
| Mencius | 01a0d0bf-ec8e-7e50-997c-24d36fae99ab | Independent bundle/mutation evidence review, complete |
| Boyle | 01a0d0c0-216e-7ae2-9bfe-cc6936edcc84 | Permanent authority instrument and mutation probes, complete |
| Descartes | 01a0d0c2-dae3-7680-b080-2d9cbc2c9806 | Independent nested-absence contract review, complete |
| Beauvoir | 01a0d0bf-ed5e-7791-ac8b-6faa145b48ee | Isolated supplied-key candidate, final NOT PROMOTABLE report found |
| Boole | 01a0d0bf-eb5c-78f2-961f-5ee6d7b12966 | Hosted boundary audit; later delegate interrupted |

No newly running command was started for this handoff. Prior known main build,
kernel, typecheck, spec-type, lint and artifact checks all completed. Process-list
inspection was denied by the local sandbox, so absence of every background
process is not claimed. Reinspect before restarting long-running work.

**Do not report all issues addressed.** There are real repaired defects and
qualified packages, preserved conformance failures, a nonpromotable concrete
candidate, and unanswered public choices. Continue from that exact distinction.
