
TRANSACTION-SEMANTICS-2 / candidate A — current implementation

  HELD        S01 one pending writer, rejected
  VIOLATED    S03 two pending, same location — reject OLDER
              rejecting the older writer destroyed the newer live value: expected y=2, got 0
  VIOLATED    S04 THREE pending, same location — reject MIDDLE
              rejecting the middle writer disturbed the newest: expected y=3, got 1
  VIOLATED    S05 reject OLDEST then settle newer — newer must not be lost
              an ACCEPTED contribution lost one of its own fields: expected y=2, got 0
  HELD        S07 reject NEWEST — older pending value survives
  VIOLATED    S11 reject both — no rejected value may remain
              both rejected, yet a rejected value is still visible: expected y=0, got 1
  HELD        S10 accept NEWER then accept OLDER — settlement must not reorder
  UNSUPPORTED A1 unrelated snapshot advances canonical, P1 stays pending
              Current implementation exposes no canonical/pending split
  UNSUPPORTED A2 correlated ACCEPT settles the contribution
              Current implementation has no correlated authority settlement input
  HELD        A5 fresh snapshot, no relation — settlement must NOT be invented
  UNSUPPORTED A6 stale versioned event must not regress canonical
              Current implementation has no revision-ordering input
  VIOLATED    F06 reject succeeds, then reject again
              a second reject reported a fresh successful settlement
  HELD        F07 reject succeeds, then accept must not resurrect

  cases: 5 held, 5 violated, 3 unsupported, 0 error, 13 total
  Held means this implemented case only; unsupported is not a pass or a law verdict.
